import {DisablableDirective} from './disablable.directive';

describe('DisablableDirective', () => {
  it('should create an instance', () => {
    const directive = new DisablableDirective();
    expect(directive).toBeTruthy();
  });
});
